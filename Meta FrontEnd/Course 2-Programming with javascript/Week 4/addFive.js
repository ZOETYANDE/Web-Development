// Installing node:
// Go on  and then download and then install nodeJs

// https://nodejs.org


// node --version

// npm --version

function addFive(val){
    return val + 5;
};

module.exports = addFive;

// in package.json change under scripts test: "jest"
// npm run test
